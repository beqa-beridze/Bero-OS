# Override: bero-os kernel 6.18.10 lacks CONFIG_USER_NS=y so PrivateUsers=yes
# fails with "Invalid argument" at user-namespace setup, killing upowerd at boot.
# Re-enable when the kernel is rebuilt with CONFIG_USER_NS=y.
[Service]
PrivateUsers=no
