# Standard system PATH with sbin dirs — sourced by login shells via /etc/profile.
# Without this, default PATH excludes /sbin and /usr/sbin (iproute, NM tools etc).
PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
export PATH
